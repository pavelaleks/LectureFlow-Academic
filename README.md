# LectureFlow Academic

Система генерации высококачественных университетских лекций на русском языке с использованием DeepSeek API и OpenAlex.

## Возможности

- 📚 Управление курсами и лекциями
- 📄 Обработка загруженных PDF/DOCX/TXT источников (приоритет)
- 🔍 Поиск научной литературы через OpenAlex
- ✍️ Генерация структурированных лекций (~4000 слов)
- 🎨 Стилистическая редактура
- 📖 Автоматическое извлечение глоссария
- 🎯 Генерация промптов для Gamma презентаций

## Установка

1. Клонируйте репозиторий или распакуйте проект

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Заполните переменные окружения в `.env`:
```
DEEPSEEK_API_KEY=your_api_key_here
OPENALEX_EMAIL=your_email@example.com
```

## Запуск

```bash
streamlit run app.py
```

Приложение откроется в браузере по адресу `http://localhost:8501`

## Использование

### 1. Создание курса

- Перейдите на страницу "Управление курсами"
- Создайте новый курс, указав ID, название и описание
- Заполните контекст курса (используется при генерации всех лекций)

### 2. Создание лекции

- Перейдите на страницу "Мастер лекций"
- Выберите курс
- Заполните метаданные лекции (название, ключевые слова и т.д.)
- Загрузите источники (PDF/DOCX/TXT) — **это приоритетный источник информации**
- Сгенерируйте библиографию через OpenAlex
- Создайте резюме библиографии
- Сгенерируйте план лекции
- Создайте черновик, затем финальную версию
- Извлеките глоссарий
- Сгенерируйте промпт для Gamma презентации

## Структура проекта

```
lectureflow_academic/
├── app.py                 # Главный файл приложения
├── config.py              # Конфигурация
├── requirements.txt       # Зависимости
├── prompts/              # Шаблоны промптов
├── src/                  # Исходный код
│   ├── llm/             # DeepSeek клиент
│   ├── openalex/        # OpenAlex клиент
│   ├── pdf/             # Обработка PDF
│   ├── core/            # Основная логика
│   ├── ui/              # Streamlit интерфейс
│   └── utils/           # Утилиты
├── data/                # Данные (курсы, загрузки)
└── outputs/             # Сгенерированные файлы
```

## Приоритет источников

При генерации лекции система использует следующую иерархию:

1. **Загруженные PDF/DOCX/TXT** (наивысший приоритет)
2. Core литература из OpenAlex
3. Recent литература из OpenAlex
4. Общие знания LLM модели

## Требования

- Python 3.8+
- DeepSeek API ключ
- Интернет-соединение (для OpenAlex и DeepSeek API)

## Лицензия

Проект создан для академического использования.

