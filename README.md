# LectureFlow Academic

Система генерации высококачественных университетских лекций на русском языке с использованием DeepSeek API и OpenAlex.

## Возможности

- 📚 Управление курсами и лекциями
- 📄 Обработка загруженных PDF/DOCX/TXT источников (приоритет)
- 🔍 Поиск научной литературы через OpenAlex
- ✍️ Генерация структурированных лекций (~4000 слов)
- 🎨 Стилистическая редактура
- 📖 Автоматическое извлечение глоссария
- 🎯 Генерация промптов для Gamma презентаций

## Требования

- **Python 3.8 или выше**
- **Git** (для клонирования репозитория)
- **DeepSeek API ключ** ([получить здесь](https://platform.deepseek.com/))
- **Email для OpenAlex** (ваш рабочий email для идентификации при запросах к OpenAlex)
- **Интернет-соединение** (для работы с API)

## Установка и развертывание

### Windows

#### 1. Установите Git (если еще не установлен)

- Скачайте Git с официального сайта: https://git-scm.com/download/win
- Запустите установщик и следуйте инструкциям
- После установки откройте **Git Bash** или **PowerShell**

#### 2. Установите Python (если еще не установлен)

- Скачайте Python с официального сайта: https://www.python.org/downloads/
- **Важно:** При установке отметьте галочку **"Add Python to PATH"**
- Убедитесь, что установлена версия 3.8 или выше:
  ```powershell
  python --version
  ```

#### 3. Клонируйте репозиторий

Откройте **PowerShell** или **Command Prompt** и выполните:

```powershell
cd C:\Users\%USERNAME%\Documents
git clone https://github.com/ваш-username/LectureFlow-Academic.git
cd LectureFlow-Academic
```

> **Примечание:** Замените `ваш-username` на ваш реальный GitHub username или используйте URL вашего репозитория.

#### 4. Создайте виртуальное окружение

```powershell
python -m venv venv
```

#### 5. Активируйте виртуальное окружение

```powershell
.\venv\Scripts\Activate.ps1
```

> **Если возникает ошибка выполнения скриптов:** Выполните в PowerShell от администратора:
> ```powershell
> Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
> ```

#### 6. Установите зависимости

```powershell
pip install --upgrade pip
pip install -r requirements.txt
```

#### 7. Создайте файл `.env`

Создайте файл `.env` в корне проекта (в папке `LectureFlow-Academic`) со следующим содержимым:

```env
DEEPSEEK_API_KEY=ваш_ключ_deepseek_здесь
OPENALEX_EMAIL=ваш_email@example.com
```

> **Важно:** 
> - Замените `ваш_ключ_deepseek_здесь` на ваш реальный API ключ от DeepSeek
> - Замените `ваш_email@example.com` на ваш реальный email адрес

### macOS

#### 1. Установите Git (если еще не установлен)

Откройте **Terminal** и выполните:

```bash
# Проверьте, установлен ли Git
git --version

# Если Git не установлен, установите через Homebrew
# Сначала установите Homebrew (если его нет):
# /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Затем установите Git:
brew install git
```

#### 2. Установите Python (если еще не установлен)

```bash
# Проверьте версию Python
python3 --version

# Если Python не установлен или версия ниже 3.8, установите через Homebrew:
brew install python@3.11
```

#### 3. Клонируйте репозиторий

Откройте **Terminal** и выполните:

```bash
cd ~/Documents
git clone https://github.com/ваш-username/LectureFlow-Academic.git
cd LectureFlow-Academic
```

> **Примечание:** Замените `ваш-username` на ваш реальный GitHub username или используйте URL вашего репозитория.

#### 4. Создайте виртуальное окружение

```bash
python3 -m venv venv
```

#### 5. Активируйте виртуальное окружение

```bash
source venv/bin/activate
```

#### 6. Установите зависимости

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

#### 7. Создайте файл `.env`

Создайте файл `.env` в корне проекта:

```bash
nano .env
```

Добавьте следующее содержимое:

```env
DEEPSEEK_API_KEY=ваш_ключ_deepseek_здесь
OPENALEX_EMAIL=ваш_email@example.com
```

Сохраните файл: нажмите `Ctrl + O`, затем `Enter`, затем `Ctrl + X`.

> **Важно:** 
> - Замените `ваш_ключ_deepseek_здесь` на ваш реальный API ключ от DeepSeek
> - Замените `ваш_email@example.com` на ваш реальный email адрес

## Запуск приложения

### Windows

Убедитесь, что виртуальное окружение активировано (в начале строки должно быть `(venv)`):

```powershell
streamlit run app.py
```

### macOS

Убедитесь, что виртуальное окружение активировано (в начале строки должно быть `(venv)`):

```bash
streamlit run app.py
```

Приложение автоматически откроется в браузере по адресу `http://localhost:8501`

> **Примечание:** Если браузер не открылся автоматически, откройте его вручную и перейдите по адресу `http://localhost:8501`

## Использование

### 1. Создание курса

- Перейдите на страницу **"Управление курсами"**
- Создайте новый курс, указав:
  - **ID курса** (латиница, без пробелов, например: `imagology2025`)
  - **Название курса**
  - **Описание курса**
- Заполните **контекст курса** — это важная информация о курсе, которая будет использоваться при генерации всех лекций (стиль, уровень сложности, специфика предмета и т.д.)

### 2. Создание лекции

- Перейдите на страницу **"Мастер лекций"**
- Выберите курс из списка
- Заполните метаданные лекции:
  - Название лекции
  - Ключевые слова (через запятую)
  - Цели лекции
- **Загрузите источники** (PDF/DOCX/TXT файлы) — **это приоритетный источник информации** для генерации
- Нажмите **"Сгенерировать библиографию"** — система найдет релевантные научные публикации через OpenAlex
- Создайте **резюме библиографии** на основе найденных источников
- Сгенерируйте **план лекции** (outline)
- Создайте **черновик лекции**
- Выполните **стилистическую редактуру** для получения финальной версии
- Извлеките **глоссарий** из лекции
- Сгенерируйте **промпт для Gamma презентации** на основе созданной лекции

### 3. Редактирование лекции

- На странице "Мастер лекций" или "Управление курсами" нажмите на существующую лекцию
- Откроется **"Редактор лекции"** с возможностью:
  - Просмотра всех версий (план, черновик, финальная версия)
  - Редактирования содержимого
  - Экспорта в DOCX
  - Повторной генерации любого этапа

## Структура проекта

```
LectureFlow-Academic/
├── app.py                      # Главный файл Streamlit приложения
├── config.py                   # Конфигурация и настройки
├── requirements.txt            # Python зависимости
├── .env                        # Переменные окружения (создается вручную)
├── prompts/                    # Шаблоны промптов для LLM
│   ├── course/
│   ├── presentation/
│   ├── steps/
│   └── system/
├── scripts/                    # Вспомогательные скрипты
│   └── export_data.py          # Скрипт экспорта/импорта данных
├── src/                        # Исходный код
│   ├── llm/                    # Клиенты для LLM (DeepSeek, Grok, OpenAI)
│   ├── openalex/               # Клиент для OpenAlex API
│   ├── pdf/                    # Обработка PDF файлов
│   ├── core/                   # Основная логика приложения
│   ├── ui/                     # Streamlit интерфейс
│   ├── storage/                # Хранение данных
│   ├── export/                 # Экспорт в различные форматы
│   └── utils/                  # Вспомогательные утилиты
├── data/                       # Данные приложения
│   ├── courses.json            # База курсов
│   ├── course_contexts/        # Контексты курсов
│   └── uploads/                # Загруженные файлы источников
└── outputs/                    # Сгенерированные файлы
    └── {course_id}/            # Файлы по курсам
        ├── {lecture_id}_outline.md
        ├── {lecture_id}_draft.md
        ├── {lecture_id}_final.md
        └── {lecture_id}_glossary.md
```

## Приоритет источников

При генерации лекции система использует следующую иерархию источников:

1. **Загруженные PDF/DOCX/TXT файлы** (наивысший приоритет) — используется содержимое из загруженных вами файлов
2. **Core литература из OpenAlex** — ключевые научные публикации по теме
3. **Recent литература из OpenAlex** — недавние публикации для актуализации
4. **Общие знания LLM модели** — базовая информация модели

## Перенос данных на другой компьютер

Если вы уже создали курсы и лекции на одном компьютере и хотите перенести их на другой (например, на MacBook после клонирования проекта), есть два способа: **автоматический (рекомендуется)** и **ручной**.

### Способ 1: Автоматический (с помощью скрипта) ⚡

#### Шаг 1: На старом компьютере — экспорт данных

Используйте скрипт для создания резервной копии всех данных:

```bash
# Windows (PowerShell)
python scripts/export_data.py export --output backup_lectureflow

# macOS (Terminal)
python3 scripts/export_data.py export --output backup_lectureflow
```

Это создаст папку `backup_lectureflow` со всеми необходимыми данными:
- `data/courses.json` — метаданные курсов и лекций
- `data/course_contexts/` — контексты курсов
- `data/uploads/` — загруженные файлы источников
- `outputs/` — все сгенерированные файлы лекций

Скопируйте эту папку на новый компьютер (через USB, облачное хранилище, сеть и т.д.).

#### Шаг 2: На новом компьютере — клонирование и настройка

Выполните стандартную установку проекта (следуйте инструкциям выше).

#### Шаг 3: На новом компьютере — размещение резервной копии

**Куда положить папку с резервной копией:**

- **Рекомендуется:** Положите папку `backup_lectureflow` в корень проекта (рядом с папками `data/` и `outputs/`)
- **Альтернатива:** Можно положить в любое удобное место (например, на Рабочий стол или в папку Документы)

**Пример структуры (рекомендуемый вариант):**

```
Documents/
└── LectureFlow-Academic/          # Проект
    ├── app.py
    ├── data/
    ├── outputs/
    ├── scripts/
    └── backup_lectureflow/        # ← Папка с резервной копией (положите сюда)
        ├── data/
        └── outputs/
```

#### Шаг 4: На новом компьютере — импорт данных

После размещения резервной копии и активации виртуального окружения:

**Если папка находится в корне проекта (рекомендуется):**

```bash
# Windows (PowerShell)
cd C:\Users\%USERNAME%\Documents\LectureFlow-Academic
.\venv\Scripts\Activate.ps1
python scripts/export_data.py import --source backup_lectureflow

# macOS (Terminal)
cd ~/Documents/LectureFlow-Academic
source venv/bin/activate
python3 scripts/export_data.py import --source backup_lectureflow
```

**Если папка находится в другом месте:**

```bash
# Windows (PowerShell) - пример, если папка на Рабочем столе
cd C:\Users\%USERNAME%\Documents\LectureFlow-Academic
.\venv\Scripts\Activate.ps1
python scripts/export_data.py import --source "C:\Users\%USERNAME%\Desktop\backup_lectureflow"

# macOS (Terminal) - пример, если папка на Рабочем столе
cd ~/Documents/LectureFlow-Academic
source venv/bin/activate
python3 scripts/export_data.py import --source ~/Desktop/backup_lectureflow
```

> **Совет:** Можно указать как относительный путь (от корня проекта), так и абсолютный путь (полный путь к папке).

Скрипт автоматически восстановит все данные. **Перезапустите Streamlit приложение** для применения изменений.

### Способ 2: Ручной перенос данных

Если вы предпочитаете скопировать данные вручную или скрипт недоступен:

#### Шаг 1: На старом компьютере — подготовка данных

Скопируйте следующие папки и файлы из проекта `LectureFlow-Academic`:

1. **`data/courses.json`** — содержит метаданные всех курсов и лекций
2. **`data/course_contexts/`** — папка с контекстными файлами курсов (`.md` файлы)
3. **`data/uploads/`** — папка с загруженными источниками (PDF/DOCX/TXT файлы)
4. **`outputs/`** — папка со всеми сгенерированными файлами лекций

### Шаг 2: Клонирование проекта на новом компьютере

Выполните стандартную установку проекта на новом компьютере (следуйте инструкциям выше).

### Шаг 3: Копирование данных

После клонирования и настройки проекта скопируйте данные в соответствующие папки:

#### Windows

```powershell
# Убедитесь, что вы в папке проекта
cd C:\Users\%USERNAME%\Documents\LectureFlow-Academic

# Скопируйте файл курсов (замените путь на путь к вашей резервной копии)
Copy-Item "путь\к\резервной\копии\courses.json" -Destination "data\courses.json" -Force

# Скопируйте папку контекстов курсов
Copy-Item "путь\к\резервной\копии\course_contexts" -Destination "data\course_contexts" -Recurse -Force

# Скопируйте папку с загруженными файлами
Copy-Item "путь\к\резервной\копии\uploads" -Destination "data\uploads" -Recurse -Force

# Скопируйте папку с выходными файлами
Copy-Item "путь\к\резервной\копии\outputs" -Destination "outputs" -Recurse -Force
```

#### macOS

```bash
# Убедитесь, что вы в папке проекта
cd ~/Documents/LectureFlow-Academic

# Скопируйте файл курсов (замените путь на путь к вашей резервной копии)
cp "путь/к/резервной/копии/courses.json" "data/courses.json"

# Скопируйте папку контекстов курсов
cp -R "путь/к/резервной/копии/course_contexts" "data/course_contexts"

# Скопируйте папку с загруженными файлами
cp -R "путь/к/резервной/копии/uploads" "data/uploads"

# Скопируйте папку с выходными файлами
cp -R "путь/к/резервной/копии/outputs" "outputs"
```

### Альтернативный способ: Использование USB-накопителя или облачного хранилища

1. **На старом компьютере:**
   - Скопируйте папки `data` и `outputs` целиком на USB-накопитель или в облачное хранилище (Google Drive, Dropbox и т.д.)

2. **На новом компьютере:**
   - После клонирования проекта скопируйте `data` и `outputs` из USB/облака в корень проекта `LectureFlow-Academic`
   - Замените существующие папки (или объедините содержимое, если там уже есть файлы)

### Структура файлов, которые нужно скопировать

```
data/
├── courses.json                    # ⚠️ ОБЯЗАТЕЛЬНО - метаданные всех курсов и лекций
├── course_contexts/                # ⚠️ ОБЯЗАТЕЛЬНО - контексты курсов
│   ├── {course_id}_context.md
│   └── ...
└── uploads/                        # ⚠️ ОБЯЗАТЕЛЬНО - загруженные источники
    ├── {course_id}/
    │   └── {lecture_id}/
    │       ├── sources.json
    │       └── [загруженные PDF/DOCX/TXT файлы]
    └── ...

outputs/                            # ⚠️ ОБЯЗАТЕЛЬНО - сгенерированные лекции
└── {course_id}/
    ├── {lecture_id}_outline.md
    ├── {lecture_id}_draft.md
    ├── {lecture_id}_final.md
    ├── {lecture_id}_glossary.md
    ├── {lecture_id}_sources.json
    ├── {lecture_id}_bibliography.json
    └── ...
```

### Важные замечания

1. **`data/courses.json`** — критически важный файл, он содержит все метаданные курсов и лекций. Без него лекции не отобразятся в интерфейсе Streamlit.

2. **Загруженные файлы** (`data/uploads/`) — если вы не скопируете эти файлы, лекции будут работать, но система не сможет использовать загруженные вами источники при повторной генерации.

3. **Сгенерированные файлы** (`outputs/`) — содержат все созданные версии лекций (планы, черновики, финальные версии, глоссарии).

4. **Контексты курсов** (`data/course_contexts/`) — содержат важную информацию о стиле и специфике каждого курса.

5. После копирования данных **перезапустите Streamlit приложение**, чтобы изменения вступили в силу.

6. Если на новом компьютере уже есть какие-то данные в этих папках, при копировании они будут заменены. Если нужно сохранить и старые, и новые данные, объедините содержимое вручную.

### Проверка после переноса

После копирования данных:

1. Запустите приложение: `streamlit run app.py`
2. Перейдите на страницу **"Управление курсами"** — вы должны увидеть все ваши курсы
3. Откройте любой курс — должны отобразиться все созданные лекции
4. Откройте лекцию — должны быть видны все версии (план, черновик, финальная версия, глоссарий)

## Устранение неполадок

### Проблема: "ModuleNotFoundError" при запуске

**Решение:** Убедитесь, что виртуальное окружение активировано и все зависимости установлены:
```bash
# Windows
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt

# macOS
source venv/bin/activate
pip install -r requirements.txt
```

### Проблема: "DEEPSEEK_API_KEY not set"

**Решение:** Проверьте, что файл `.env` существует в корне проекта и содержит правильный API ключ:
```env
DEEPSEEK_API_KEY=ваш_ключ
OPENALEX_EMAIL=ваш_email
```

### Проблема: Ошибки при обработке PDF

**Решение:** Убедитесь, что установлены все зависимости:
```bash
pip install --upgrade pdfplumber pymupdf
```

### Проблема: Приложение не открывается в браузере

**Решение:** Откройте браузер вручную и перейдите по адресу `http://localhost:8501`

## Лицензия

Проект создан для академического использования.

## Поддержка

При возникновении проблем проверьте:
1. Версию Python (`python --version` или `python3 --version`)
2. Активировано ли виртуальное окружение
3. Наличие файла `.env` с правильными ключами
4. Наличие интернет-соединения для работы с API
